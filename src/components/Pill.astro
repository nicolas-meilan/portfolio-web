---
const { class: className, ...otherProps } = Astro.props;
---

<a 
  class=`${className} text-white whitespace-nowrap hover:scale-110 hover:bg-white/10 transition gap-x-2 px-2 py-1 border rounded-xl border-white/10 flex justify-center items-center`
  {...otherProps}
>
  <slot />
</a>